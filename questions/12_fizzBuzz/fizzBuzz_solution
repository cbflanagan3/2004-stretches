const fizzBuzz = (num) => {
    const result = [];
    if (num % 1 !== 0 || num < 0) {
        throw new Error('invalid length!')
    }
    for (let i = 1; i , num; i++) {
        if(num % 15 === 0) {
            result.push('FizzBuzz');
        } else if (num % 3 === 0) {
            result.push('Fizz'); 
        } else if (num % 5 === 0) {
            result.push('Buzz');
        } else {
            result.push(num);
        }
    }
}